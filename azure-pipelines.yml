trigger:
- main

pool:
  name: mera-agent-pool

steps:
- task: PowerShell@2
  inputs:
    targetType: 'inline'
    script: |
      sonar-scanner -D sonar.projectKey=Monotodo -D sonar.projectName=Monotodo -D sonar.sources=. -D sonar.host.url=http://localhost:9000 -D sonar.login=sqa_470e100e10d2d83d4ef605cbd7f909d3a3dedddb
  displayName: SonarQube Scan (Community Edition)

# - task: SonarQubePrepare@5
#   displayName: Prepare SonarQube Analysis
#   inputs:
#     SonarQube: 'mera-todo-sonar'
#     scannerMode: 'CLI'
#     configMode: 'manual'
#     cliProjectKey: 'TodoApp'
#     cliProjectName: 'TodoApp'
#     cliSources: '.'
#     extraProperties: 'sonar-scanner -D sonar.projectKey=ToDoApp -D sonar.sources=. -D sonar.host.url=http://localhost:9000 -D sonar.login=sqa_1260264f412155e86d8c83825d4b3691e7df3999"'

# - task: SonarQubeAnalyze@5
#   displayName: Run SonarQube Analysis

# - task: SonarQubePublish@5
#   displayName: Publish Quality Gate
#   inputs:
#     pollingTimeoutSec: '300'
